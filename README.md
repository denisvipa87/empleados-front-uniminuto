# CONSULTAEMPLEADOS

Este proyecto fue creado con [Angular CLI](https://github.com/angular/angular-cli) version 15.2.10.

## Install node modules required

1. Ejecuta `npm install` para instalar los paquetes de node requeridos

2. Ejecute el comando `ng serve -o` para levantar el proyecto